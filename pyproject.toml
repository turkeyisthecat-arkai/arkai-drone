[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "arkai-drone"
version = "0.1.0"
description = "Starter autonomous AI drone architecture for Jetson + PX4/ArduPilot"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
